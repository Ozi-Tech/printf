#include "holberton.h"
/**
 * _printf - produces output according to a set format
 * @format: character string
 * Return: the number of characters printed (excluding NULL)
 */

int _printf(const char *format, ...)
{
    
}
